import nltk
import os

Tagslist = []

class Tag: # category, cluster, class. E.g. author.
	import nltk
	import os
	def __init__(self, **kwargs):
		self.__dict__ = args = {type : 'unknown', name : 'unknown', order : 1}
		self.__dict__ = kwargs
		### check text file for existing <Tag>, else append to it

	def learn
	
class Classification:
	import nltk
	import os
	def __init__(self, **kwargs):
		self.__dict__ = kwargs
		
	def computeScore(**kwargs):
		args = {}
